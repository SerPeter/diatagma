"""Core library — shared by MCP server and web API.

All file I/O, parsing, caching, graph resolution, and priority computation
lives here. The MCP and web layers are thin wrappers around this package.
"""
